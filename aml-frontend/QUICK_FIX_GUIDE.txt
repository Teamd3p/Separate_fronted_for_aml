═══════════════════════════════════════════════════════════════
  SUSPICIOUS KEYWORDS CRUD - QUICK FIX GUIDE
═══════════════════════════════════════════════════════════════

PROBLEM:
  ❌ "Could not commit JPA transaction" error on Update/Create/Delete

ROOT CAUSE:
  1. Frontend was sending wrong field names
  2. Backend missing @Transactional annotation

═══════════════════════════════════════════════════════════════
  FRONTEND FIX (ALREADY DONE ✅)
═══════════════════════════════════════════════════════════════

Changed payload from:
  { keyword: "...", isActive: true }
  
To:
  { word: "...", active: true }

═══════════════════════════════════════════════════════════════
  BACKEND FIX (REQUIRED ⚠️)
═══════════════════════════════════════════════════════════════

FILE: AdminServiceImpl.java

ADD THIS LINE above each method:
  @Transactional

EXAMPLE:
  
  @Transactional  ← ADD THIS
  @Override
  public SuspiciousKeyword updateKeyword(Long id, KeywordRequest request) {
      SuspiciousKeyword kw = keywordRepo.findById(id)
          .orElseThrow(() -> new RuntimeException("Keyword not found"));
      kw.setWord(request.getWord());
      kw.setCategory(request.getCategory());
      kw.setSeverity(request.getSeverity());
      kw.setActive(request.getActive());
      return keywordRepo.save(kw);
  }

ADD TO THESE METHODS:
  ✓ createKeyword()
  ✓ updateKeyword()
  ✓ deleteKeyword()

═══════════════════════════════════════════════════════════════
  STEPS TO FIX
═══════════════════════════════════════════════════════════════

1. Open: AdminServiceImpl.java
2. Add import: import org.springframework.transaction.annotation.Transactional;
3. Add @Transactional above createKeyword, updateKeyword, deleteKeyword
4. Save file
5. Restart backend server
6. Test CRUD operations in frontend

═══════════════════════════════════════════════════════════════
  VERIFICATION
═══════════════════════════════════════════════════════════════

After backend fix:
  ✅ Create keyword - Should work
  ✅ Update keyword - Should work (no transaction error)
  ✅ Toggle status - Should work
  ✅ Delete keyword - Should work

═══════════════════════════════════════════════════════════════

For detailed explanation, see: BACKEND_FIX_REQUIRED.md
For complete summary, see: CRUD_FIX_SUMMARY.md
